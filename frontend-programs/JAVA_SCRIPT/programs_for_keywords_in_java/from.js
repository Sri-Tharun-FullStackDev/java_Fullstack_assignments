// In JavaScript, the from keyword is primarily used in conjunction with the import statement when working with ES6 modules. It specifies the module from which to import functions, objects, or primitives.

import { name, greet } from './module.js';
console.log(name);
console.log(greet());
