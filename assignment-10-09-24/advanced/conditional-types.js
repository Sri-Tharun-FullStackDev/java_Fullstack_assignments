// Example usage of StringifiedUser type
var user = {
    id: "123", // now a string
    name: "Alice", // already a string
    age: "30", // now a string
    isActive: "true" // now a string
};
console.log(user);
